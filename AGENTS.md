# MD Bot Repo Notes

This repository is the `MD Bot` codebase, currently located at `/Users/kwekuaboagye/src/md-one`.

Work only inside this repository unless the user explicitly asks for cross-repo work.

Do not edit `/Users/kwekuaboagye/src/worship-flow` by inference. That is a separate project, even though the older name appears in some historical docs and cloud resource identifiers.

Use `MD Bot` for user-facing copy and `md-bot` for internal slugs and metadata unless the user asks for a different convention.