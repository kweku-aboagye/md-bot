[build]
buildCommand = "npm ci && cd client && npm ci && npm run build && cd .. && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
